tls:
  certificates:
    - certFile: "/etc/traefik/certs/airflow.crt"
      keyFile: "/etc/traefik/certs/airflow.key"

  stores:
    default:
      defaultCertificate:
        certFile: "/etc/traefik/certs/airflow.crt"
        keyFile: "/etc/traefik/certs/airflow.key"
